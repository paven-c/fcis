<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fancy.module.agent.repository.mapper.AgMerchantOrderDetailMapper">

  <select id="listMerchantTaskNum" resultType="com.fancy.module.agent.controller.vo.MerchantTaskNumListVO">
    select sum(service_total_num) as service_total_num,
           sum(service_consume_num) as service_consume_num, content_service_id
    from ag_merchant_order_detail
    where
    deleted = 0
    and service_status in (0,2)
    <if test="merchantId != null">
      and merchant_id = #{merchantId}
    </if>
    group by content_service_id
  </select>

</mapper>
